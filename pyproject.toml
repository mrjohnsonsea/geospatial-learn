[project]
name = "geospatial-learn"
version = "0.1.0"
description = "Project for learning to work with geospatial data."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "ipywidgets>=8.1.8",
    "kagglehub>=1.0.2",
]
